"use client";

// Simple component to pass through children
// We're not using SessionProvider anymore since we've moved to a custom auth implementation
interface AuthProviderProps {
  children: React.ReactNode;
}

export default function AuthProvider({ children }: AuthProviderProps) {
  return <>{children}</>;
}
